/**
 * CLI-layer defaults. These values are presentation concerns and intentionally
 * kept out of the Core layer to respect the project's architectural boundaries.
 */

/**
 * Default CLI output format.
 */
export const DEFAULT_OUTPUT_FORMAT: 'console' | 'json' = 'console';
